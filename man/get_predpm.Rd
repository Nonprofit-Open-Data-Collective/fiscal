% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-predpm.R
\name{get_predpm}
\alias{get_predpm}
\title{Pre-Depreciation Profitability Margin}
\usage{
get_predpm( df,
  revenue      = "F9_08_REV_TOT_TOT",
  expenses     = "F9_09_EXP_TOT_TOT",
  depreciation = "F9_09_EXP_DEPREC_TOT",
  numerator = NULL, denominator = NULL, winsorize = 0.98,
  sanitize  = TRUE,
  summarize = FALSE )
}
\arguments{
\item{df}{A \code{data.frame} containing the fields required for computing the metric.}

\item{revenue}{Total revenue. (On 990: Part VIII, line 12A; \code{F9_08_REV_TOT_TOT})}

\item{expenses}{Total functional expenses. (On 990: Part IX, line 25A; \code{F9_09_EXP_TOT_TOT})}

\item{depreciation}{Depreciation, depletion, and amortization. (On 990: Part IX, line 22A; \code{F9_09_EXP_DEPREC_TOT})}

\item{numerator}{Optional. A pre-calculated column for the numerator
(revenue minus non-depreciation expenses). Cannot be combined with
\code{revenue}, \code{expenses}, or \code{depreciation}.}

\item{denominator}{Optional. A pre-calculated column for the denominator. Cannot be
combined with \code{revenue}.}

\item{winsorize}{The winsorization value (between 0 and 1), defaults to 0.98, which
winsorizes at the 1st and 99th percentiles.}

\item{sanitize}{Logical (default \code{TRUE}). If \code{TRUE}, NA values in
the financial input columns are imputed to zero before the ratio is computed,
respecting form scope: Part X and VIII/IX fields (990 only) are imputed only
for 990 filers; Part I summary fields (990 + 990EZ) are imputed for all filers.
The returned dataframe always contains the original unmodified input columns.}

\item{summarize}{Logical. If \code{TRUE}, prints a \code{summary()} of
the results and plots density curves for all four output columns
(raw, winsorized, z-score, percentile). Defaults to \code{FALSE}.}
}
\value{
Object of class \code{data.frame}: the original dataframe appended with four
new columns:
\itemize{
\item \code{predpm}   — pre-depreciation profitability margin (raw)
\item \code{predpm_w} — winsorized version
\item \code{predpm_z} — standardized z-score (based on winsorized values)
\item \code{predpm_p} — percentile rank (1-100)
}
}
\description{
Operating surplus or deficit as a share of total revenue, before depreciation.

\strong{Formula:}

\if{html}{\out{<div class="sourceCode">}}\preformatted{predpm = ( total_revenue - ( total_expenses - depreciation ) ) / total_revenue
}\if{html}{\out{</div>}}

\strong{Calculated For:} 990 filers only.
}
\details{
The pre-depreciation profitability margin adds back depreciation to measure the
cash-based surplus as a share of revenue. Because depreciation is a non-cash expense,
this metric more closely approximates actual cash flow than \code{\link{get_podpm}}.
Higher values relative to the post-depreciation margin reflect the impact of large
non-cash depreciation charges.

\strong{Variables used:}
\itemize{
\item \code{F9_08_REV_TOT_TOT}: Total revenue (\code{revenue})
\item \code{F9_09_EXP_TOT_TOT}: Total functional expenses (\code{expenses})
\item \code{F9_09_EXP_DEPREC_TOT}: Depreciation, depletion, and amortization (\code{depreciation})
}
}
\examples{
library( fiscal )
data( dat10k )

d <- get_predpm( df = dat10k )
head( d[ , c( "predpm", "predpm_w", "predpm_z", "predpm_p" ) ] )

}
