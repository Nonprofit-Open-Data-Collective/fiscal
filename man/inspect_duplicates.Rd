% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-dedupe.R
\name{inspect_duplicates}
\alias{inspect_duplicates}
\title{Inspect duplicate filings before deduplication}
\usage{
inspect_duplicates(
  df,
  by_id = "EIN2",
  by_year = "TAX_YEAR",
  col_group = "RETURN_GROUP_X",
  col_partial = "RETURN_PARTIAL_X",
  col_amended = "RETURN_AMENDED_X",
  col_stamp = "RETURN_TIME_STAMP",
  return_candidates = FALSE
)
}
\arguments{
\item{df}{A \code{data.frame} containing IRS 990 e-file data.}

\item{by_id}{Name of the organization identifier column. Default \code{"EIN2"}.}

\item{by_year}{Name of the tax year column. Default \code{"TAX_YEAR"}.}

\item{col_group}{Name of the group return flag column. Default \code{"RETURN_GROUP_X"}.}

\item{col_partial}{Name of the partial-year return flag column.
Default \code{"RETURN_PARTIAL_X"}.}

\item{col_amended}{Name of the amended return flag column.
Default \code{"RETURN_AMENDED_X"}.}

\item{col_stamp}{Name of the filing timestamp column.
Default \code{"RETURN_TIME_STAMP"}.}

\item{return_candidates}{Logical. If \code{TRUE}, return the full duplicate
candidate table with ranking columns. Default is \code{FALSE}.}
}
\value{
A list with:
\describe{
\item{summary_types}{Count of duplicated candidate rows by filing type, with total, kept, and dropped counts.}
\item{summary_sizes}{Count of duplicated org-years by number of filings.}
\item{summary_size_by_type}{Count of duplicated candidate rows by number of filings and filing type, with total, kept, and dropped counts.}
\item{examples}{One representative duplicated org-year for each number of filings, including all candidate rows in that example group.}
\item{candidates}{Optional full duplicate candidate table, returned only if \code{return_candidates = TRUE}.}
}
}
\description{
Diagnoses duplicated organization-year filings using the same ranking logic
as \code{deduplicate()}. For each duplicated \code{by_id} x \code{by_year} group, the
function identifies the row that would be retained and classifies each
candidate filing by filing type.

Filing types are:
\itemize{
\item \code{"normal"}: not amended, not group, not partial
\item \code{"partial"}: partial only
\item \code{"amended"}: amended only
\item \code{"group"}: group only
\item \code{"amended_partial"}: amended + partial
\item \code{"amended_group"}: amended + group
\item \code{"group_partial"}: group + partial
\item \code{"amended_group_partial"}: amended + group + partial
}

The retained filing is indicated separately by the logical column
\code{retained}.
}
