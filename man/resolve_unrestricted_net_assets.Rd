% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{resolve_unrestricted_net_assets}
\alias{resolve_unrestricted_net_assets}
\title{Resolve unrestricted net assets with a fallback for non-SFAS 117 filers}
\usage{
resolve_unrestricted_net_assets(
  dat,
  unrestricted = "F9_10_NAFB_UNRESTRICT_EOY",
  restricted = "F9_10_NAFB_RESTRICT_EOY",
  total = "F9_10_NAFB_TOT_EOY",
  verbose = TRUE
)
}
\arguments{
\item{dat}{A \code{data.frame}.}

\item{unrestricted}{Column name(s) for unrestricted net assets (line 27).}

\item{restricted}{Column name(s) for restricted net assets (line 28), or
\code{NULL}. When \code{NULL} or absent from \code{dat}, restricted net assets are treated
as zero, so the fallback applies wherever unrestricted net assets are zero.}

\item{total}{Column name(s) for total net assets (line 33), or \code{NULL} to
disable the fallback.}

\item{verbose}{Logical. Print the number of substituted rows. Default \code{TRUE}.}
}
\value{
A numeric vector of length \code{nrow(dat)}.
}
\description{
Part X lines 27-28 (net assets without / with donor restrictions) are
completed only by organizations that follow SFAS 117 (ASC 958). Other filers
report equity on lines 30-32 (capital stock, paid-in surplus, retained
earnings), so their unrestricted net assets read as zero even when total net
assets (line 33) are large. Without donor-restriction reporting, that equity
is effectively unrestricted.

This helper returns unrestricted net assets, substituting total net assets
for rows where unrestricted \strong{and} restricted net assets are both zero.
Rows where unrestricted net assets are \code{NA} (e.g. 990-EZ filers) are left
\code{NA}.
}
\examples{
d <- data.frame( F9_10_NAFB_UNRESTRICT_EOY = c( 100, 0, 0 ),
                 F9_10_NAFB_RESTRICT_EOY   = c(  50, 0, 80 ),
                 F9_10_NAFB_TOT_EOY        = c( 150, 900, 80 ) )
resolve_unrestricted_net_assets( d )   # 100 900 0
}
