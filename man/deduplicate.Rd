% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-dedupe.R
\name{deduplicate}
\alias{deduplicate}
\title{Deduplicate a panel to one filing per organization per year}
\usage{
deduplicate(
  df,
  by_id = "EIN2",
  by_year = "TAX_YEAR",
  col_group = "RETURN_GROUP_X",
  col_partial = "RETURN_PARTIAL_X",
  col_amended = "RETURN_AMENDED_X",
  col_stamp = "RETURN_TIME_STAMP",
  verbose = TRUE
)
}
\arguments{
\item{df}{A \code{data.frame} containing IRS 990 e-file data.}

\item{by_id}{Name of the organization identifier column. Default \code{"EIN2"}.}

\item{by_year}{Name of the tax year column. Default \code{"TAX_YEAR"}.}

\item{col_group}{Name of the group return flag column. Default \code{"RETURN_GROUP_X"}.}

\item{col_partial}{Name of the partial-year return flag column.
Default \code{"RETURN_PARTIAL_X"}.}

\item{col_amended}{Name of the amended return flag column. Default \code{NULL}.
If \code{NULL} or not found in \code{df}, amended-return priority is skipped.}

\item{col_stamp}{Name of the filing timestamp column.
Default \code{"RETURN_TIME_STAMP"}.}

\item{verbose}{Logical. If \code{TRUE} (default), print a deduplication report.}
}
\value{
A \code{data.frame} with the same columns as \code{df} and at most one row
per \code{by_id} x \code{by_year} combination.
}
\description{
IRS 990 e-file panels can contain multiple filings for the same
organization-year due to group returns, partial-year filings, amended
returns, and repeated submissions. \code{deduplicate()} reduces the data to
exactly one record per \code{by_id} x \code{by_year} combination by assigning each
candidate filing a priority rank and keeping the best-ranked row within
each organization-year.

\strong{Priority rules (applied jointly):}
\enumerate{
\item Prefer non-group returns over group returns.
\item Prefer non-partial returns over partial-year returns.
\item Prefer amended returns over non-amended returns when an amended flag
column is available.
\item Prefer the most recent filing timestamp when a timestamp column is
available.
\item Break any remaining ties by original row order.
}

This approach guarantees that at least one record is kept for every
observed organization-year, even if all available filings are group
returns, partial returns, or have missing timestamps.
}
\details{
The function uses \code{data.table} internally for speed on large panels.

The timestamp column is parsed with \code{as.POSIXct()}. If parsing fails for
some rows, those rows are treated as having missing timestamps and lose the
timestamp tie-break unless all competitors are also missing timestamps.

If no amended-return column is supplied, the function still prefers the
newest timestamp after accounting for group and partial return status.
}
