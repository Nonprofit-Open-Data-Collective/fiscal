% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-panel.R
\name{deduplicate}
\alias{deduplicate}
\title{Deduplicate a panel to one filing per organization per year}
\usage{
deduplicate(df, by_id = "EIN2", by_year = "TAX_YEAR", verbose = TRUE)
}
\arguments{
\item{df}{A \code{data.frame} containing IRS 990 efile data with one or more
rows per organization-year. Typically the output of \code{\link[=get_panel]{get_panel()}}.}

\item{by_id}{Name of the column identifying the organization. Default
\code{"EIN2"}.}

\item{by_year}{Name of the column identifying the tax year. Default
\code{"TAX_YEAR"}.}

\item{verbose}{Logical. If \code{TRUE} (default), prints a deduplication
report showing records dropped per step and the distribution of
duplicates per organization per year.}
}
\value{
A \code{data.frame} with the same columns as \code{df} and at most one
row per \code{by_id} x \code{by_year} combination. Row names are reset.
}
\description{
IRS 990 efile data can contain multiple records for the same organization
in a single tax year due to amended returns, partial-year filings, and
group returns. \code{deduplicate()} reduces the data to at most one record
per \code{by_id} x \code{by_year} combination using a three-step heuristic, then
prints a summary report of how many organizations and records were
affected.

\strong{Deduplication heuristics (applied in order):}
\enumerate{
\item \strong{Drop group returns} — filings where \code{RETURN_GROUP_X == "X"} are
removed first. Group returns aggregate activity across multiple
subordinate organizations and are not suitable as individual-organization
observations in a panel.
\item \strong{Drop partial-year returns} — filings where \code{RETURN_PARTIAL_X == "X"}
are removed next. Partial-year returns cover less than a full fiscal
year and are not comparable to full-year filings for the same period.
\item \strong{Keep most recent filing} — when multiple records remain for the
same organization-year, the filing with the latest \code{RETURN_TIME_STAMP}
is retained. This is typically the most recently filed amended return,
which supersedes earlier submissions for the same period.
}

\strong{Safety guarantee:} at least one record per organization-year is always
retained. If applying step 1 or 2 would eliminate \emph{all} records for a
given organization-year (e.g., an organization that only filed a group
return), those records are kept and a warning is issued.

\strong{Missing flag columns:} if \code{RETURN_GROUP_X}, \code{RETURN_PARTIAL_X}, or
\code{RETURN_TIME_STAMP} are absent from \code{df}, the corresponding step is
skipped with a message.
}
\details{
\subsection{Deduplication report}{

When \code{verbose = TRUE}, the function prints:
\itemize{
\item Starting dimensions (rows and unique org-years).
\item Records dropped at each step (group returns, partial-year returns,
timestamp tie-breaking), with counts per year.
\item A frequency table of how many \emph{extra} records existed per
organization-year before deduplication (e.g., "42 org-years had
2 total filings, 7 had 3 total filings").
\item Final dimensions after deduplication.
}
}

\subsection{When to call this function}{

Call \code{deduplicate()} on the raw output of \code{\link[=get_panel]{get_panel()}} before passing
the panel to \code{\link[=panel_smooth]{panel_smooth()}} or \code{\link[=compute_all]{compute_all()}}, since both assume
one observation per organization-year.
}
}
\examples{
\dontrun{
panel <- get_panel( years = 2019:2022 )
panel_clean <- deduplicate( panel )
dim( panel_clean )

# Suppress the report
panel_clean <- deduplicate( panel, verbose = FALSE )
}

}
\seealso{
\code{\link[=get_panel]{get_panel()}}, \code{\link[=panel_smooth]{panel_smooth()}}
}
