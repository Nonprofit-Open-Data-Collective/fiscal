% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-describe.R
\name{panel_composition}
\alias{panel_composition}
\title{Summarize panel composition over time}
\usage{
panel_composition(df, time = "TAX_YEAR", id = "EIN2")
}
\arguments{
\item{df}{A data frame containing panel data.}

\item{time}{Name of the time variable. Default is \code{"TAX_YEAR"}.}

\item{id}{Name of the organization identifier variable. Default is \code{"EIN2"}.}
}
\value{
A data frame with one row per year and columns:
\describe{
\item{year}{Panel year.}
\item{balanced}{Count of balanced organizations, repeated in each year.}
\item{entrants}{Count of entrant organizations in the year they first appear.}
\item{exits}{Count of exit organizations in the year they last appear.}
\item{interlopers}{Count of interloper organizations observed in each year.}
}

The returned object also includes an attribute:

\describe{
\item{classification}{A data frame with one row per ID and columns for ID, group, first year, and last year.}
}
}
\description{
Classifies organizations in a panel into four groups:
}
\details{
\itemize{
\item balanced: present in every period
\item entrants: enter after the first panel year and remain through the end
\item exits: present in the first panel year and leave before the final year
\item interlopers: observed intermittently, with one or more missing years in the middle
}

If an organization could qualify as an entrant or exit but has a gap in the
middle of its observed years, it is classified as an interloper.

The returned table has one row per year and four count columns:
\itemize{
\item \code{balanced}: repeated in every year, equal to the total number of balanced organizations
\item \code{entrants}: counted only in the year of first appearance
\item \code{exits}: counted only in the year of last appearance
\item \code{interlopers}: counted in each year they are observed
}

A detailed ID-level classification table is attached as the
\code{"classification"} attribute of the returned data frame.
}
\examples{
df <- data.frame(
  EIN2 = c(
    "A","A","A","A",
    "B","B","B",
    "C","C","C",
    "D","D","D"
  ),
  TAX_YEAR = c(
    2021,2022,2023,2024,
    2022,2023,2024,
    2021,2022,2023,
    2021,2023,2024
  )
)

out <- panel_composition( df )
out

attr( out, "classification" )

}
