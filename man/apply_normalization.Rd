% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize-x.R
\name{apply_normalization}
\alias{apply_normalization}
\title{Apply a fitted normalization to a numeric vector}
\usage{
apply_normalization(x, fit, verbose = FALSE)
}
\arguments{
\item{x}{Numeric vector to score.}

\item{fit}{A \code{normalize_x_fit} object returned by \code{\link[=find_best_normalization]{find_best_normalization()}}.}

\item{verbose}{Logical. Print a brief summary. Default \code{FALSE}.}
}
\value{
A numeric vector of the same length as \code{x} with transformation
metadata attached as attributes (\code{transform_type}, \code{transform_label},
\code{transform_note}, \code{guess_reason}, \code{center}, \code{scale}, \code{range},
\code{winsorize}, \code{offset}, \code{fit_object}).
}
\description{
Given a \code{normalize_x_fit} object produced by \code{\link[=find_best_normalization]{find_best_normalization()}},
re-winsorizes \code{x} using the same settings, applies the fitted
transformation, and standardizes using the fitted center and scale.

Using the fitted ECDF / parameters rather than re-fitting ensures that new
observations (e.g. from a different year) are scored on the same scale as
the training data.
}
\examples{
x_train <- c( rnorm(200), rep(0, 30) )
x_new   <- rnorm(50)

# fit on training data, score new data with same parameters
fit  <- find_best_normalization( x_train, range = "np", verbose = FALSE )
z    <- apply_normalization( x_new, fit = fit )
}
\seealso{
\code{\link[=find_best_normalization]{find_best_normalization()}}, \code{\link[=normalize_x]{normalize_x()}}
}
