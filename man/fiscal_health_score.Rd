% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fiscal-health-score.R
\name{fiscal_health_score}
\alias{fiscal_health_score}
\title{Apply a fitted fiscal-health measurement model}
\usage{
fiscal_health_score(
  data,
  fit,
  missing = c("fit", "median", "complete"),
  min_coverage = 0,
  prefix = "fh_",
  include_standalone = TRUE,
  include_coverage = TRUE,
  append = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{data}{A data frame containing every indicator required by \code{fit}.}

\item{fit}{A \code{fiscal_health_fit} object.}

\item{missing}{Missing-value rule. \code{"fit"} reuses the rule selected during
fitting; \code{"median"} uses the reference-sample medians stored in \code{fit};
\code{"complete"} returns a missing dimension score if any indicator in that
dimension is missing.}

\item{min_coverage}{Minimum proportion of a dimension's source indicators
that must be observed before a score is reported. This applies even when
reference medians are used.}

\item{prefix}{Prefix applied to generated score columns.}

\item{include_standalone}{Include standardized standalone indicators.}

\item{include_coverage}{Add an observation-level coverage column for every
generated measure.}

\item{append}{If \code{TRUE}, append scores to \code{data}; otherwise return score and
coverage columns only.}

\item{overwrite}{Allow generated columns to replace existing columns.}
}
\value{
A data frame of class \code{fiscal_health_scores}. Its attributes retain
the fitted model, scoring audit, and generated column names.
}
\description{
Scores reference or new data using the transformations and coefficients
stored by \code{\link[=fiscal_health_fit]{fiscal_health_fit()}}. No model parameters are re-estimated.
}
