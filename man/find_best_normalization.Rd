% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize-x.R
\name{find_best_normalization}
\alias{find_best_normalization}
\title{Fit normalization parameters on the stable interior of a distribution}
\usage{
find_best_normalization(
  x,
  range,
  vtype = NULL,
  winsorize = 0.98,
  offset = 0.001,
  standardize = TRUE,
  robust = TRUE,
  zero_tol = 1e-08,
  one_tol = 1e-08,
  boundary_mass_cutoff = 0.1,
  hurdle_trans = c("rank", "logit"),
  hurdle_range = c(1, 100),
  verbose = TRUE
)
}
\arguments{
\item{x}{Numeric vector to fit on.}

\item{range}{Character range code (\code{"np"}, \code{"zp"}, \code{"zo"}, \code{"nz"}, or
\code{"lo;hi"}). Required.}

\item{vtype}{Optional transformation type override. One of \code{NULL}
(auto-detect), \code{"asinh"}, \code{"logit"}, \code{"rank_normal"}, \code{"hurdle"}.}

\item{winsorize}{Winsorization proportion. Default \code{0.98}.}

\item{offset}{Sentinel offset for fixed bounds. Default \code{0.001}.}

\item{standardize}{Logical. If \code{TRUE}, fit centering and scaling constants.
Default \code{TRUE}.}

\item{robust}{Logical. If \code{TRUE}, use median/MAD; otherwise mean/SD.
Default \code{TRUE}.}

\item{zero_tol}{Tolerance for zero detection. Default \code{1e-8}.}

\item{one_tol}{Tolerance for near-one detection. Default \code{1e-8}.}

\item{boundary_mass_cutoff}{Hurdle/rank_normal trigger threshold. Default
\code{0.10}.}

\item{hurdle_trans}{Positive-part transformation for hurdle variables.
\code{"rank"} (default) or \code{"logit"}.}

\item{hurdle_range}{Rescaled output range for hurdle positive component.
Default \code{c(1, 100)}.}

\item{verbose}{Logical. If \code{TRUE}, print a diagnostic summary. Default
\code{TRUE}.}
}
\value{
An object of class \code{normalize_x_fit} — a named list containing
transformation type, parameters, centering/scaling constants, and
diagnostics. Pass this object to \code{\link[=apply_normalization]{apply_normalization()}}.
}
\description{
Winsorizes \code{x}, isolates the stable (non-sentinel, non-NA) interior,
selects a transformation type, fits its parameters and centering/scaling
constants, and returns a \code{normalize_x_fit} object that can be reused via
\code{\link[=apply_normalization]{apply_normalization()}}.

Separating fitting from scoring allows the same fitted model to be applied
to new data (e.g. a holdout year) without re-estimating parameters, and
ensures that sentinel observations do not distort the center and spread
estimates used for standardization.
}
\examples{
x <- c( rnorm(200), rep(0, 30) )

# fit on training data
fit <- find_best_normalization( x, range = "np", verbose = FALSE )
fit$transform_type
fit$center
fit$scale
}
\seealso{
\code{\link[=apply_normalization]{apply_normalization()}}, \code{\link[=normalize_x]{normalize_x()}}
}
