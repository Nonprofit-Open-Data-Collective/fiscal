% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fiscal-diagnostics.R
\name{fiscal_diagnostics}
\alias{fiscal_diagnostics}
\title{Diagnose candidate fiscal-health indicators}
\usage{
fiscal_diagnostics(
  data,
  variables = NULL,
  corr_cutoff = 0.9,
  kmo_low = 0.5,
  communality_low = 0.2,
  mean_abs_cor_low = 0.1,
  smc_high = 0.9,
  vif_high = 10,
  near_zero_sd = 1e-06,
  nfactors_comm = NULL,
  rotate = "oblimin",
  fm = "minres",
  use = "pairwise.complete.obs"
)
}
\arguments{
\item{data}{A data frame containing candidate indicators.}

\item{variables}{Character vector of columns to diagnose. By default, all
numeric columns are used.}

\item{corr_cutoff}{Absolute correlation used to flag redundancy.}

\item{kmo_low}{KMO/MSA threshold used to flag weak shared variance.}

\item{communality_low}{Threshold used to flag low communality.}

\item{mean_abs_cor_low}{Threshold used to flag weak connectedness.}

\item{smc_high}{Squared multiple correlation threshold.}

\item{vif_high}{Variance inflation factor threshold.}

\item{near_zero_sd}{Standard-deviation threshold for near-zero variance.}

\item{nfactors_comm}{Number of factors in the preliminary communality
screen. The default is between one and three, based on the number of
usable indicators.}

\item{rotate}{Rotation passed to \code{\link[psych:fa]{psych::fa()}}.}

\item{fm}{Factoring method passed to \code{\link[psych:fa]{psych::fa()}}.}

\item{use}{Missing-value rule passed to \code{\link[stats:cor]{stats::cor()}}.}
}
\value{
An object of class \code{fiscal_diagnostics}. Its \code{variables} component
is a variable-level data frame; other components contain the correlation
matrix, redundancy pairs, thresholds, preliminary model, and warnings.
}
\description{
Computes variable-level diagnostics used to distinguish indicators that
participate in a shared multivariate structure from redundant, standalone,
or noisy indicators. Correlation summaries exclude each variable's
correlation with itself.
}
\details{
\code{psych} is used, when installed, for KMO/MSA and a preliminary common-factor
communality screen. The remaining diagnostics are always computed. If
\code{psych} is unavailable or the factor screen fails, the relevant fields are
returned as \code{NA} and the reason is recorded in \code{warnings}.
}
