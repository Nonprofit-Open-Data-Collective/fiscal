% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-panel.R
\name{get_panel}
\alias{get_panel}
\title{Retrieve a multi-year IRS 990 efile panel dataset}
\usage{
get_panel(
  years,
  tables = c("P00", "P01", "P08", "P09", "P10"),
  include_bmf = TRUE,
  efile_root = "https://nccs-efile.s3.us-east-1.amazonaws.com/public/efile_v2_1/",
  bmf_url =
    "https://nccsdata.s3.us-east-1.amazonaws.com/bmf/unified/v1.2/UNIFIED_BMF_V1.2.csv",
  timeout = 300,
  retry_max = 3L,
  verbose = TRUE
)
}
\arguments{
\item{years}{Integer vector of tax years to retrieve (e.g.,
\code{2019:2023}).}

\item{tables}{Character vector of efile table codes passed to
\code{\link[=retrieve_efile_data]{retrieve_efile_data()}}. Default \code{c("P00","P01","P08","P09","P10")}.}

\item{include_bmf}{Logical. Attach BMF organizational metadata after all
years are stacked. Default \code{TRUE}. The BMF is downloaded exactly once
regardless of how many years are requested, then filtered to only the
EINs present in the assembled panel before joining — keeping memory use
proportional to the panel rather than to the full BMF.}

\item{efile_root}{Base URL for the NCCS efile S3 bucket. Passed
directly to \code{\link[=retrieve_efile_data]{retrieve_efile_data()}}.}

\item{bmf_url}{Full URL for the Unified BMF CSV. Passed directly to
\code{\link[=retrieve_efile_data]{retrieve_efile_data()}}.}

\item{timeout}{HTTP timeout in seconds for each file download. Default
\code{300}.}

\item{retry_max}{Integer. Maximum download attempts per efile table.
Default \code{3}. Passed directly to \code{\link[=retrieve_efile_data]{retrieve_efile_data()}}. A failed
table is retried with a 1–5 second random back-off between attempts;
if it still fails the table is skipped and a warning is issued, but
the year's other tables are unaffected.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{data.frame} in long format with one row per
organization-year. The \code{TAX_YEAR} column identifies the filing year.
Columns that exist in some years but not others are filled with \code{NA}.
}
\description{
Downloads individual years of IRS 990 efile data using
\code{\link[=retrieve_efile_data]{retrieve_efile_data()}}, adds a \code{TAX_YEAR} column to each year's data
frame, then stacks them into a single long-format panel.

\strong{Calculated For:} 990 + 990EZ filers (determined by the underlying
efile tables requested).
}
\details{
\subsection{Year loop and column alignment}{

Each year is downloaded independently and stacked with
\code{\link[dplyr:bind_rows]{dplyr::bind_rows()}}, which handles mismatched columns across years by
filling missing columns with \code{NA}. This is the expected behaviour when
the IRS changes form schemas between years.
}

\subsection{BMF join}{

When \code{include_bmf = TRUE}, the BMF is downloaded exactly \strong{once} after
all years have been retrieved and stacked. Before joining, the BMF is
filtered to \code{bmf$EIN2 \%in\% unique(panel$EIN2)} so that only the
organizations actually present in the panel are kept in memory.
This avoids both repeated large downloads across years and holding the
full ~200 MB BMF in memory alongside the panel.
}
}
\examples{
\dontrun{
# Retrieve a three-year panel
panel <- get_panel( years = 2019:2021 )
dim( panel )
table( panel$TAX_YEAR )

# Retrieve without BMF (faster; attach manually later)
panel <- get_panel( years = 2019:2021, include_bmf = FALSE )
}

}
\seealso{
\code{\link[=retrieve_efile_data]{retrieve_efile_data()}}, \code{\link[=panel_smooth]{panel_smooth()}},
\code{\link[=sanitize_financials]{sanitize_financials()}}, \code{\link[=compute_all]{compute_all()}}
}
