% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-panel.R
\name{panel_smooth}
\alias{panel_smooth}
\title{Smooth financial variables across time within a panel dataset}
\usage{
panel_smooth(
  df,
  vars = "PZ",
  window = 3,
  time = "TAX_YEAR",
  id = "EIN2",
  weights = "equal",
  verbose = TRUE
)
}
\arguments{
\item{df}{A \code{data.frame} containing a panel of financial data with one
row per organization-year.}

\item{vars}{Which financial variables to smooth. One of:
\describe{
\item{\code{"PZ"}}{Smooth all PZ-scope fields returned by
\code{\link[=get_pz_fields]{get_pz_fields()}} that are present in \code{df}.}
\item{\code{"PC"}}{Smooth all PC-scope fields returned by
\code{\link[=get_pc_fields]{get_pc_fields()}} that are present in \code{df}.}
\item{\code{"ALL"}}{Smooth all PC and PZ fields present in \code{df}.}
\item{character vector}{A custom list of column names to smooth.
All names must be present in \code{df}.}
}}

\item{window}{Odd integer >= 1. Width of the rolling window in time
periods. Default \code{3} (current year plus one year on each side).
\code{window = 1} returns the data unchanged.}

\item{time}{Name of the column identifying the time period. Default
\code{"TAX_YEAR"}.}

\item{id}{Name of the column identifying the organization. Default
\code{"EIN2"}.}

\item{weights}{Weighting scheme for the rolling window. One of:
\describe{
\item{\code{"equal"}}{Uniform weights — each observation in the window
contributes equally. Equivalent to a simple moving average.}
\item{\code{"half"}}{The center observation receives weight 1/2; the
remaining 1/2 is split equally among all other observations in
the window. Emphasizes the focal year while incorporating
neighbouring context.}
\item{\code{"decay"}}{Half-life exponential decay from the center:
distance 0 → weight 1, distance 1 → 1/2, distance 2 → 1/4,
etc. Strongly emphasizes the focal year.}
}}

\item{verbose}{Logical. If \code{TRUE} (default), prints a one-line
progress message at the start and completion of each year's
smoothing pass, including the number of organizations and columns
being processed. Useful for monitoring progress on large panels.
Set to \code{FALSE} to suppress all messages.}
}
\value{
A \code{data.frame} with the same dimensions and column names as
\code{df}. The columns identified by \code{vars} contain smoothed values;
all other columns (identifiers, non-numeric, excluded fields) are
unchanged.
}
\description{
Applies a centered rolling window average within each organization's
time series to reduce year-to-year noise in financial variables. The
data frame is returned with the same dimensions and column names; only
the selected financial columns are replaced with their smoothed values.

\strong{Window behavior at panel edges:} When an observation is within
\code{floor(window/2)} periods of the start or end of an organization's
series, the window shifts to stay within the available data rather than
shrinking. This means every smoothed value is based on exactly
\code{min(window, n)} observations, where \code{n} is the panel length for that
organization.

\strong{Missing values:} \code{NA} observations are dropped from the window
before computing the weighted average. Their weight is reallocated
proportionally to the remaining non-missing observations so the weights
always sum to 1. If all observations in a window are \code{NA}, the smoothed
value is \code{NA}.

\strong{\code{NaN} values} (produced by \code{\link[=get_panel]{get_panel()}} ratio functions for
structurally undefined observations, e.g. division by zero) are treated
as \code{NA} during smoothing and remain \code{NaN} in the output for those
positions.
}
\details{
\subsection{Choosing a weighting scheme}{

\code{"equal"} is the simplest and most transparent option — it is the
standard moving average and makes no assumptions about the relative
importance of focal vs. neighbouring years.

\code{"half"} is a reasonable default for nonprofit financial ratios where
the reported year is the most informative but a single year's value
is known to be noisy (e.g., due to one-time gifts or accrual timing).

\code{"decay"} is appropriate when you believe recent-year data is
progressively more informative than distant years — for instance when
smoothing raw financial levels that have secular trends.
}

\subsection{Panel length and the window}{

Organizations with fewer years than \code{window} are smoothed over their
full available history rather than being dropped or truncated. This
means short panels are smoothed more aggressively than long ones when
the same \code{window} is requested. Use \code{window = 1} to apply no smoothing
to any organization.
}

\subsection{Row order}{

The output rows are returned in the same order as the input \code{df}.
}
}
\examples{
\dontrun{
panel <- get_panel( years = 2019:2022 )

# Smooth all PZ-scope fields (available for 990 + 990EZ filers)
panel_s <- panel_smooth( panel, vars = "PZ" )

# Smooth a custom set of columns with decay weighting
panel_s <- panel_smooth(
  panel,
  vars    = c( "F9_01_REV_TOT_CY", "F9_01_EXP_TOT_CY" ),
  window  = 5,
  weights = "decay"
)
}

}
\seealso{
\code{\link[=get_panel]{get_panel()}}, \code{\link[=get_pz_fields]{get_pz_fields()}}, \code{\link[=get_pc_fields]{get_pc_fields()}}
}
