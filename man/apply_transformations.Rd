% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{apply_transformations}
\alias{apply_transformations}
\title{Apply winsorization, normalization, and percentile ranking to a ratio vector}
\usage{
apply_transformations(
  x,
  winsorize = 0.98,
  offset = 0.001,
  range = "np",
  normalize_type = NULL
)
}
\arguments{
\item{x}{Numeric vector (the computed ratio, before any transformation).}

\item{winsorize}{Winsorization proportion between 0 and 1 (default \code{0.98},
which clips at the 1st and 99th percentiles for \code{"np"} range).}

\item{offset}{Sentinel offset applied to fixed bounds (default \code{0.001}).
Observations clipped to a fixed bound are stored as \verb{bound ± offset} so
they remain identifiable in the \verb{_w} column.}

\item{range}{Character string describing the theoretical range of the ratio.
Controls how the lower and upper winsorization bounds are determined:
\describe{
\item{\code{"np"}}{Negative to positive (unbounded both directions). Winsorizes
symmetrically at the \code{(1-winsorize)/2} and \code{1-(1-winsorize)/2}
percentiles. Default behaviour.}
\item{\code{"zp"}}{Zero to positive. The lower bound is fixed at \code{-offset}
(flagging truncated-at-zero values) and the upper bound is the
\code{winsorize} percentile of the full distribution.}
\item{\code{"zo"}}{Zero to one. Both bounds are fixed (\code{-offset} and
\code{1+offset}), flagging values outside \verb{[0, 1]}. No percentile-based
clipping is applied.}
\item{\code{"nz"}}{Negative to zero. The upper bound is fixed at \code{+offset}
and the lower bound is the \code{1-winsorize} percentile.}
\item{\code{"lo;hi"}}{Custom numeric range, e.g. \code{"0;10"}. The lower bound
is fixed at \code{lo - offset} and the upper bound at \code{hi + offset}.}
}}

\item{normalize_type}{Transformation type override passed to
\code{\link[=find_best_normalization]{find_best_normalization()}}. One of \code{NULL} (auto-detect), \code{"asinh"},
\code{"logit"}, \code{"rank_normal"}, or \code{"hurdle"}. Default \code{NULL}.}
}
\value{
A named list with elements \code{raw}, \code{winsorized}, \code{z}, \code{pctile}.
}
\description{
\code{apply_transformations()} is the central post-computation step called by
every \verb{get_*()} ratio function. It produces four versions of a ratio:
\itemize{
\item \strong{raw} (\verb{_raw}): the unmodified computed ratio.
\item \strong{winsorized} (\verb{_w}): outliers clipped to bounds determined by the
\code{range} argument and the \code{winsorize} proportion, via \code{\link[=winsorize_x]{winsorize_x()}}.
\item \strong{normalized} (\verb{_z}): a distribution-appropriate transformation of the
winsorized values. Parameters are fitted on the stable interior (non-NA,
non-sentinel observations) via \code{\link[=find_best_normalization]{find_best_normalization()}}, then scored
on the full vector via \code{\link[=apply_normalization]{apply_normalization()}}, so sentinel pile-up at
winsorization bounds does not distort the centering and spread estimates.
\item \strong{percentile} (\verb{_p}): integer percentile rank (1-100) based on the raw
values, via \code{\link[dplyr:ntile]{dplyr::ntile()}}.
}
}
\details{
\strong{Winsorization}

Delegated entirely to \code{\link[=winsorize_x]{winsorize_x()}}, which handles all range codes,
computes sentinel flags, and returns the winsorized vector alongside
diagnostic metadata.

\strong{Normalization (\verb{_z} column)}

\code{\link[=find_best_normalization]{find_best_normalization()}} is called first to fit transformation
parameters (type, scale constant, center, spread) on the stable interior
of the winsorized distribution. \code{\link[=apply_normalization]{apply_normalization()}} then scores the
full original vector using those fitted parameters. This two-step design
means the fitted model can be reused on new data if needed.

\strong{Percentile rank (\verb{_p} column)}

Integer percentile rank from 1 to 100 based on the raw (pre-winsorized)
values, computed with \code{\link[dplyr:ntile]{dplyr::ntile()}}.
}
\examples{
library( fiscal )
data( dat10k )

# winsorize and normalize the debt-to-assets ratio
ratio <- dat10k$F9_10_LIAB_TOT_EOY / dat10k$F9_10_ASSET_TOT_EOY
out   <- apply_transformations( ratio, winsorize = 0.98, range = "zo" )
names( out )
summary( out$z )
}
\seealso{
\code{\link[=winsorize_x]{winsorize_x()}}, \code{\link[=find_best_normalization]{find_best_normalization()}},
\code{\link[=apply_normalization]{apply_normalization()}}, \code{\link[=normalize_x]{normalize_x()}}, \code{\link[=plot_normalize_x]{plot_normalize_x()}}
}
