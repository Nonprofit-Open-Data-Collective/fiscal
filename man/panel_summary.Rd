% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-describe.R
\name{panel_summary}
\alias{panel_summary}
\title{Print a panel composition summary from appended classification columns}
\usage{
panel_summary(df, time = "TAX_YEAR", id = "EIN2", print_table = TRUE)
}
\arguments{
\item{df}{A data frame with \code{panel_type} and the \code{time} column present.}

\item{time}{Name of the time variable column. Default \code{"TAX_YEAR"}.}

\item{id}{Name of the organization identifier column. Default \code{"EIN2"}.}

\item{print_table}{Logical. If \code{TRUE}, print the formatted table.
Default \code{TRUE}.}
}
\value{
A data frame with one row per panel year and one column per
panel type, invisibly. Same structure as the summary table returned
by \code{panel_composition()}.
}
\description{
Produces a year-by-type count table from a data frame that already has
\verb{panel_*} columns appended by \code{panel_composition(append_classification = TRUE)}. Useful for quickly re-checking composition after imputation or
other panel manipulations without re-running the full classification.
}
\examples{
\dontrun{
d <- panel_composition(d, append_classification = TRUE)
d <- panel_impute(d)
panel_summary(d)
}

}
\seealso{
\code{\link[=panel_composition]{panel_composition()}}
}
