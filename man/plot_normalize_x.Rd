% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize-x.R
\name{plot_normalize_x}
\alias{plot_normalize_x}
\title{Plot raw and normalized versions of a numeric vector}
\usage{
plot_normalize_x(
  x,
  range = NULL,
  normalize = TRUE,
  breaks = 40,
  col_raw = "steelblue",
  col_norm = "firebrick",
  border = "white",
  main_raw = "Raw",
  main_norm = NULL,
  ...
)
}
\arguments{
\item{x}{A numeric vector (raw input, or already-normalized if
\code{normalize = FALSE}).}

\item{range}{Character range code passed to \code{\link[=normalize_x]{normalize_x()}} when
\code{normalize = TRUE}. Required unless \code{normalize = FALSE}.}

\item{normalize}{Logical. If \code{TRUE} (default), normalize \code{x} inside the
function. If \code{FALSE}, treat \code{x} as already normalized.}

\item{breaks}{Number of histogram breaks. Default \code{40}.}

\item{col_raw}{Fill color for the raw histogram. Default \code{"steelblue"}.}

\item{col_norm}{Fill color for the normalized histogram. Default
\code{"firebrick"}.}

\item{border}{Bar border color. Default \code{"white"}.}

\item{main_raw}{Title for the raw panel. Default \code{"Raw"}.}

\item{main_norm}{Optional title for the normalized panel. If \code{NULL}, uses
the \code{transform_label} attribute when available.}

\item{...}{Additional arguments passed to \code{\link[=normalize_x]{normalize_x()}} when
\code{normalize = TRUE}.}
}
\value{
Invisibly returns a list with elements \code{raw} and \code{normalized}.
}
\description{
Diagnostic companion to \code{\link[=normalize_x]{normalize_x()}}. Plots side-by-side histograms
of the raw and normalized distributions.
}
\seealso{
\code{\link[=normalize_x]{normalize_x()}}, \code{\link[=find_best_normalization]{find_best_normalization()}}
}
