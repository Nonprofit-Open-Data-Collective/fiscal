% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize-x.R
\name{winsorize_x}
\alias{winsorize_x}
\title{Winsorize a numeric vector according to a range specification}
\usage{
winsorize_x(x, range, winsorize = 0.98, offset = 0.001)
}
\arguments{
\item{x}{Numeric vector (NAs are ignored).}

\item{range}{Character range code (\code{"np"}, \code{"zp"}, \code{"zo"}, \code{"nz"}, or
\code{"lo;hi"}).}

\item{winsorize}{Winsorization proportion (default \code{0.98}).}

\item{offset}{Sentinel offset for fixed bounds (default \code{0.001}).}
}
\value{
A named list with elements:
\itemize{
\item \code{x_w} — winsorized vector
\item \code{top}, \code{bottom} — the computed bounds
\item \code{is_top_sentinel}, \code{is_bottom_sentinel}, \code{is_sentinel} — logical vectors
\item \code{support} — parsed range specification list
\item \code{winsorize}, \code{offset} — echoed inputs
}
}
\description{
Clips \code{x} to \verb{[bottom, top]} bounds determined by \code{range} and \code{winsorize},
flags which observations were reassigned to a sentinel bound, and returns
a list suitable for downstream normalization.
}
\examples{
x <- c( NA, rnorm(200), 100, -100 )

# winsorize with symmetric np bounds
w <- winsorize_x( x, range = "np", winsorize = 0.98 )
range( w$x_w, na.rm = TRUE )
sum( w$is_sentinel )
}
