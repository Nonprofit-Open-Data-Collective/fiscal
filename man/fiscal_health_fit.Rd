% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fiscal-health-fit.R
\name{fiscal_health_fit}
\alias{fiscal_health_fit}
\title{Fit a reusable fiscal-health measurement model}
\usage{
fiscal_health_fit(
  data,
  dimensions,
  dimension_names = NULL,
  scoring = "standardized_mean",
  standalone = NULL,
  indicator_direction = NULL,
  missing = c("median", "complete"),
  min_indicators = 2L,
  fm = "minres",
  strict_direction = FALSE
)
}
\arguments{
\item{data}{Reference data used to estimate the measurement model.}

\item{dimensions}{A \code{fiscal_dimensions} object or a named list whose elements
are character vectors of indicators.}

\item{dimension_names}{Optional named character vector mapping discovered
dimension names to substantive names.}

\item{scoring}{Dimension scoring method. Supply one value for every dimension
or a named vector. Supported methods are \code{"standardized_mean"}, \code{"pca"},
and \code{"factor"}.}

\item{standalone}{Optional character vector of standalone indicators. The
default uses standalones carried by a \code{fiscal_dimensions} object.}

\item{indicator_direction}{Optional named vector overriding indicator
directions with \code{"higher"}, \code{"lower"}, or \code{"context"}. Other directions
are inferred from \code{\link[=fiscal_metrics]{fiscal_metrics()}} when possible.}

\item{missing}{Training-data missing-value rule: \code{"median"} or
\code{"complete"}. Median replacement here is statistical missing-value
treatment, not e-file data normalization.}

\item{min_indicators}{Minimum number of indicators required in a modeled
dimension.}

\item{fm}{Factoring method passed to \code{\link[psych:fa]{psych::fa()}} for factor scores.}

\item{strict_direction}{If \code{TRUE}, error when an indicator is context
dependent or absent from the metric registry without an explicit override.}
}
\value{
An object of class \code{fiscal_health_fit} containing dimension and
standalone scoring specifications, metadata, warnings, and the source
discovery object.
}
\description{
Converts reviewed candidate dimensions into a reusable scoring
specification. The fitted object stores indicator orientation, centers,
scales, missing-value treatment, dimension coefficients, and dimension-score
standardization parameters. It can therefore be applied unchanged to later
panel years by a scoring function.
}
