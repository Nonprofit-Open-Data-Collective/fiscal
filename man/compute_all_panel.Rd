% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute-all.R
\name{compute_all_panel}
\alias{compute_all_panel}
\title{Compute all fiscal health metrics year-by-year across a panel}
\usage{
compute_all_panel(
  df,
  year = "TAX_YEAR",
  metrics = c("ratio", "w", "z", "p"),
  append_to_df = TRUE,
  winsorize = 0.98,
  verbose = TRUE
)
}
\arguments{
\item{df}{A \code{data.frame} containing IRS 990 efile financial fields
for multiple tax years. Must contain the column named by \code{year}.}

\item{year}{Name of the column that identifies the tax year. Default
\code{"TAX_YEAR"}.}

\item{metrics}{Character vector of metric variants to return. Any
combination of \code{"ratio"}, \code{"w"}, \code{"z"}, \code{"p"}. Default: all four.}

\item{append_to_df}{Logical. If \code{TRUE} (default), returns the original
\code{df} with metric columns appended. If \code{FALSE}, returns only
\code{.IDVARS} identifier columns plus the selected metric columns.}

\item{winsorize}{Winsorization proportion (default \code{0.98}).}

\item{verbose}{Logical. If \code{TRUE} (default), prints per-year progress
from \code{\link[=compute_all]{compute_all()}}.}
}
\value{
A \code{data.frame} with the same row order as \code{df} and the same
metric columns that \code{\link[=compute_all]{compute_all()}} would produce, but with
\verb{_w} / \verb{_z} / \verb{_p} values computed within each year's
cross-sectional distribution.
}
\description{
A panel-safe wrapper around \code{\link[=compute_all]{compute_all()}} that splits the data by
year, computes every fiscal health metric independently within each
year's slice, then stacks the results. This ensures that
winsorization bounds, normalization parameters, and percentile ranks
are all estimated from the cross-sectional distribution of a \strong{single
year} rather than from the pooled multi-year distribution.

\strong{Why year-by-year computation matters:}

\code{\link[=compute_all]{compute_all()}} passes each raw ratio vector through
\code{\link[=apply_transformations]{apply_transformations()}}, which fits winsorization bounds and a
normalization model on the data it receives. On a pooled panel those
distributions are a mixture of N years, so:
\itemize{
\item A ratio at the 95th percentile in 2021 may rank at the 80th
percentile in the pooled distribution if 2022–2024 values are higher.
\item Winsorization clips to pooled extremes, not year-specific extremes.
\item Z-scores are centred and scaled on pooled moments.
}

Splitting by year before computing avoids all three problems: every
\verb{_w}, \verb{_z}, and \verb{_p} column is interpretable as a within-year
percentile or standardised value, exactly as it would be for a
single-year call to \code{\link[=compute_all]{compute_all()}}.
}
\details{
\subsection{Implementation}{

Internally the function:
\enumerate{
\item Validates that \code{year} is present and that the panel contains more
than one year (a single-year panel works but a warning is issued
since \code{\link[=compute_all]{compute_all()}} would give identical results).
\item Tags each row with its original position so output rows can be
restored to the input order after splitting and stacking.
\item Splits \code{df} into a list of single-year slices using
\code{data.table::split()}.
\item Calls \code{\link[=compute_all]{compute_all()}} on each slice with \code{verbose} passed through
so per-year messages are visible (or suppressed) as requested.
\item Stacks the per-year results with \code{data.table::rbindlist()}, which
handles any columns that are present in some years but absent in
others (e.g. if a 990EZ-only year lacks Part VIII columns) by
filling with \code{NA}.
\item Restores the original row order and drops the internal row-tag
column.
}
}

\subsection{Panel size considerations}{

On large panels (e.g. 5 years × 50 000 orgs) the per-year loop runs
\code{\link[=compute_all]{compute_all()}} five times on ~50 000-row slices rather than once on
a 250 000-row frame. Each slice call is faster and uses less peak
memory; the \code{data.table::rbindlist()} stack is O(N) with no copies.
}
}
\examples{
\dontrun{
panel <- get_panel( years = 2019:2022 )
panel <- deduplicate( panel )

# Year-safe metric computation
panel_ratios <- compute_all_panel( panel )

# Compare: without year splitting (pooled — incorrect for _w/_z/_p)
panel_ratios_pooled <- compute_all( panel )

# Check that percentile ranks are within-year:
library( data.table )
setDT( panel_ratios )
panel_ratios[ , range(debt_assets_p), by = TAX_YEAR ]
# All years should show 1 to 100.
}

}
\seealso{
\code{\link[=compute_all]{compute_all()}}, \code{\link[=get_panel]{get_panel()}}, \code{\link[=deduplicate]{deduplicate()}},
\code{\link[=panel_smooth]{panel_smooth()}}
}
