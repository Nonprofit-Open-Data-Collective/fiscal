% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize-x.R
\name{normalize_x}
\alias{normalize_x}
\title{Normalize a numeric vector for multivariate analysis}
\usage{
normalize_x(
  x,
  range,
  vtype = NULL,
  winsorize = 0.98,
  offset = 0.001,
  standardize = TRUE,
  robust = TRUE,
  zero_tol = 1e-08,
  one_tol = 1e-08,
  boundary_mass_cutoff = 0.1,
  hurdle_trans = c("rank", "logit"),
  hurdle_range = c(1, 100),
  verbose = TRUE
)
}
\arguments{
\item{x}{A numeric vector.}

\item{range}{Character range code (\code{"np"}, \code{"zp"}, \code{"zo"}, \code{"nz"}, or
\code{"lo;hi"}). Required.}

\item{vtype}{Optional transformation override. One of \code{NULL}, \code{"asinh"},
\code{"logit"}, \code{"rank_normal"}, \code{"hurdle"}.}

\item{winsorize}{Winsorization proportion. Default \code{0.98}.}

\item{offset}{Sentinel offset for fixed bounds. Default \code{0.001}.}

\item{standardize}{Logical. Standardize after transformation. Default \code{TRUE}.}

\item{robust}{Logical. Use median/MAD standardization. Default \code{TRUE}.}

\item{zero_tol}{Tolerance for zero detection. Default \code{1e-8}.}

\item{one_tol}{Tolerance for near-one detection. Default \code{1e-8}.}

\item{boundary_mass_cutoff}{Share of values at zero (or one) above which a
[0, 1] variable is given \code{"rank_normal"} instead of \code{"logit"}. Default
\code{0.10}.}

\item{hurdle_trans}{Positive-part method for hurdle variables (\code{"rank"} or
\code{"logit"}). Default \code{"rank"}.}

\item{hurdle_range}{Rescaled range for hurdle positive component. Default
\code{c(1, 100)}.}

\item{verbose}{Print diagnostic summaries. Default \code{TRUE}.}
}
\value{
A numeric vector of the same length as \code{x}, with transformation
metadata attached as attributes (see \code{\link[=apply_normalization]{apply_normalization()}}).
}
\description{
Convenience wrapper that calls \code{\link[=find_best_normalization]{find_best_normalization()}} and
\code{\link[=apply_normalization]{apply_normalization()}} in a single step. For repeated application of
the same fitted model use those functions directly.

The function winsorizes \code{x} according to \code{range} and \code{winsorize}, isolates
the stable interior (non-sentinel, non-NA values) to fit transformation
parameters, then scores all observations -- including sentinels -- using
those fitted parameters. This prevents boundary pile-up at winsorization
bounds from distorting the centering and spread estimates (for
\code{"rank_normal"} the reference distribution keeps the winsorized tails; see
below).

Four transformation types are supported:
\itemize{
\item \code{"asinh"}: inverse hyperbolic sine, scaled by the median absolute
nonzero value of the stable interior. Defined at zero, accommodates
negatives, and behaves like log in the tails.
\item \code{"logit"}: maps the stable interior to [0, 1] relative to its
theoretical support, then applies \code{qlogis()}. Suited to variables bounded
in [0, 1] without heavy boundary mass.
\item \code{"rank_normal"}: rank-based inverse normal transform. Preserves ordering,
not distances. Robust to irregular shapes and mixed support.
\item \code{"hurdle"}: structural-zero mass receives 0; positive values are
transformed separately (rank or logit) and rescaled to a positive interval.
}

When \code{vtype = NULL} the type is auto-detected from the stable interior using
heuristics based on the support range and boundary mass (\code{"hurdle"} is only
used when requested explicitly).

Scores stay bounded at the edges of the distribution so that they preserve
the rank correlations of the raw data:
\itemize{
\item \code{"rank_normal"} uses mid-ranks, so a pile-up of tied values (e.g. exact
zeros, or program-expense ratios of exactly one) is scored at the middle
of its block, and winsorized tails are scored at their true quantile.
\item \code{"logit"} clamps to the \code{(1 - winsorize) / 2} quantiles of the strictly
interior values, so values at 0 or 1 are not sent to \verb{+/- log(2n)}.
\item When more than half the transformed values are tied, the MAD is zero and
robust standardization falls back to mean/SD instead of returning \code{NA}.
}
}
\examples{
x <- c( NA, rnorm(200), rep(0, 20), 50, -50 )

# one-step normalize with auto-detected transformation
z <- normalize_x( x, range = "np", verbose = FALSE )
hist( z, breaks = 30, main = "Normalized" )

# force a specific transformation
z_asinh <- normalize_x( x, range = "np", vtype = "asinh", verbose = FALSE )
}
\seealso{
\code{\link[=find_best_normalization]{find_best_normalization()}}, \code{\link[=apply_normalization]{apply_normalization()}},
\code{\link[=plot_normalize_x]{plot_normalize_x()}}
}
