% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{impute_zero}
\alias{impute_zero}
\title{Impute zero for NA values in financial fields, respecting form scope}
\usage{
impute_zero(dat, vars, ez_rows)
}
\arguments{
\item{dat}{A \code{data.frame} (the working subset, not the full dataset).}

\item{vars}{Character vector of financial column names to consider.}

\item{ez_rows}{Logical vector (length \code{nrow(dat)}) marking 990-EZ rows,
as returned by \code{\link{detect_ez_rows}}.}
}
\value{
The modified \code{data.frame} with zeros imputed where appropriate.
}
\description{
For a given set of column names and a data frame, imputes zero for NA
values in financial fields while respecting whether a field is available
to 990-EZ filers. PC-scope fields (990 only) are imputed only for rows
that are not 990-EZ filers. PZ-scope fields (990 + 990-EZ) are imputed
for all rows. Rows where every financial variable is NA are left untouched.
}
\examples{
data( dat10k )
ez <- detect_ez_rows( dat10k )
dat_imp <- impute_zero( dat10k, vars = get_pc_fields(), ez_rows = ez )
}
